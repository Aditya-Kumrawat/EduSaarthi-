# .env.example - Copy this to .env and fill in your actual values
# .env is better suited for public variables, ie, variables that should not be committed
# For secret variables it's better to use DevServerControl tool with set_env_variable: ["KEY", "SECRET"]

# https://www.builder.io/c/docs/using-your-api-key
VITE_PUBLIC_BUILDER_KEY=your_builder_public_key_here
PING_MESSAGE="ping pong"

# Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=your_gemini_api_key_here
